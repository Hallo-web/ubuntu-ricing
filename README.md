# ubuntu-ricing
To Extract:
unzip lofi-gnome-dotfiles.zip -d ~/lofi-gnome-dotfiles
cd ~/lofi-gnome-dotfiles
chmod +x install.sh

To Install:
./install.sh --nature   # or --coder

To Revert:
./install.sh --remove
